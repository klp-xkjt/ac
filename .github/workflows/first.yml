name: My First Action!
env: 
    NAME: Action-learning

on: 
    push:
        branches: ["main", "test"]
    pull_request:
        branches: ["main", "test"]
jobs: 
    my_first_job:
        runs-on: ubuntu-latest
        steps: 
            - name: Say Hello World
              run: echo "Hello World! This is ${{env.NAME}}!"
            - name: Checkout
              uses: actions/checkout@v7
            - name: Tree
              run: tree
            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
            - name: Clippy
              run: cargo clippy
            - name: Check it!
              run: cargo test
            - name: Run it!
              run: cargo run
        